{
  "name": "tg-fin-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Telegram <-> Intercom Fin bridge on Cloudflare Workers (iGaming Phase 1)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240605.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.60.0"
  }
}
